import React from 'react';
import Icons from '../Icons/Icons'

const MadeBy = () => {
    const icons = [
        'tutorial', 
        'self',
    ];

    return (
        <>
            <Icons icons={icons}/>
        </>
    );
}


export default MadeBy;